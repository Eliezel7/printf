#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>
#include <stddef.h>
#include <stdarg.h>

int _putchar(char c);
int printf_ch(va_list val);
int _printf(const char *format, ...);
int printf_stringg(va_list val);
int _strlen(char *st);
int _strlenc(const char *st);


#endif
